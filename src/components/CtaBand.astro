---
const { title, text, href, label = 'Open tool' } = Astro.props;
---
<section class="cta-band">
  <div>
    <h2>{title}</h2>
    <p>{text}</p>
  </div>
  <a class="btn btn-primary" href={href}>{label}</a>
</section>
